Git

Version control -- Maintain History

pull - get local copy
push - add the changes

git add 
git commit
git push

Git - version control system (VCS) - track the changes
    - COllaborate in teams
    - Distributed VCS
    - Open Source and Free

VCS - Centralized VCS    | Distributed VCS

Github - upload to online repository
       - Provide back up
 git and github both are different

GIT Comments

git init - initializing
git status - find the status

git add - to add the file

git remote add orgin https://github.com/abirami3/first.git
git commit

git config --global user.name ''
git config --global user.email ''

git rm --cached filename -- will removed added file from staged

git add *.html (Add all the html file) / . / *.* (add all the file)

git commit -m ''

.gitignore will ignore the file in the folder


git branch first_branch  // create the branch

git checkout master/branch_name  //chnage to nxt branch

git merge branch_name -m 'message' //merge the branch

git remote   // remote repositories

git remote add repo_name url(github repo url)  // remote to created origin

get push -u repo_name branch_name

git clone clone_url

git pull

git push -u origin branch_name // push the barnch to remote

git barnch -d barnch_name  // delete barnch from local

git push origin --delete branch_name  //delete from remote

git tag tag_name

git push -u origin barnch_name tag_name



